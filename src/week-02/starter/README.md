# Week 2 Starter

Open `index.html` in a browser. During the guided build, add meaningful HTML
and connect it to `styles/style.css`.

The Starter deliberately contains only the document scaffold. It gives every
learner the same reliable starting point without hiding the HTML and CSS they
are here to learn.
